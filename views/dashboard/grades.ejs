<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title><%= title %></title>
  <style>
    *{margin:0;padding:0;box-sizing:border-box}
    body{font-family:Arial;background:#f9fafb;padding:18px}
    table{width:100%;border-collapse:collapse}
    th,td{padding:8px;border-bottom:1px solid #eee}
    .muted{color:#6b7280}
  </style>
</head>
<body>
  <h2>My Grades</h2>
  <p class="muted">Student: <strong><%= user && user.name %></strong></p>
  <table>
    <thead><tr><th>Assignment</th><th>Subject</th><th>Submitted</th><th>Marks</th><th>Feedback</th></tr></thead>
    <tbody>
      <% if (!submissions || submissions.length === 0) { %>
        <tr><td colspan="5">No submissions yet</td></tr>
      <% } else { %>
        <% submissions.forEach(s=>{ %>
          <tr>
            <td><%= (s.assignment && s.assignment.title) ? s.assignment.title : '—' %></td>
            <td><%= s.assignment && s.assignment.subject ? s.assignment.subject : '—' %></td>
            <td><%= new Date(s.submittedAt || s.createdAt).toLocaleString() %></td>
            <td><%= s.marks != null ? s.marks : 'Not graded' %></td>
            <td><%= s.feedback || '-' %></td>
          </tr>
        <% }) %>
      <% } %>
    </tbody>
  </table>
</body>
</html>
